use bevy::prelude::*;

pub mod components;
pub mod math;
pub mod systems;

pub struct StarSystemPlugin;

impl Plugin for StarSystemPlugin {
    fn build(&self, app: &mut App) {
        app
            
        ;
    }
}